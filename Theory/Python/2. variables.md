
---

# 🧠 Переменные

## 📌 Что такое переменная?

**Переменная** — это "имя", которое указывает на некоторый объект в памяти.  
В Python переменная не "содержит" значение, а **ссылается** на объект.

```python
x = 5
```

Здесь:
- `x` — имя переменной.
- `5` — объект типа `int`, который хранится в памяти.
- `x` ссылается на этот объект.

---

## ⚙️ Объявление переменной

В Python переменные создаются автоматически при первом присваивании:

```python
name = "Alice"
age = 30
pi = 3.14
```

Не нужно указывать тип (как в C++ или Java), потому что Python — **динамически типизированный язык**.

---

## 🔁 Динамическая типизация

Ты можешь переприсвоить переменной другое значение, даже другого типа:

```python
x = 10
x = "десять"  # теперь x — строка
```

Это может быть удобно, но и коварно: теряется контроль над типами.

---

## 🧾 Именование переменных

- Название должно начинаться с буквы или `_`, но не с цифры.
- Может содержать буквы, цифры, подчёркивания.
- Чувствительно к регистру: `age`, `Age` и `AGE` — разные имена.
- Названия должны быть **понятными**: `user_name` лучше, чем `x`.

🔴 **Нельзя** использовать ключевые слова Python (например, `if`, `for`, `class`, `True` и т.п.)

---

## 🧪 Проверка типа — `type()`

```python
x = 42
print(type(x))  # <class 'int'>
```

Полезно при отладке и в валидации.

---

## 🔄 Присваивание нескольких значений

```python
a, b = 1, 2
x = y = z = 0  # все три переменные указывают на одно значение 0
```

---

## 🧷 Распаковка коллекций

```python
data = [10, 20, 30]
a, b, c = data
```

Можно использовать `*` для "остатка":

```python
a, *rest = [1, 2, 3, 4]
# a = 1, rest = [2, 3, 4]
```

---

## 🧠 Область видимости (Scope)

**Скоуп** определяет, где переменная "видна" и доступна. В Python есть 4 уровня:

### LEGB Rule:

1. **Local** — внутри текущей функции
2. **Enclosing** — во вложенной функции (замыкание)
3. **Global** — на уровне модуля
4. **Built-in** — встроенные имена (`len`, `print`, `sum` и т.д.)

### Пример:

```python
x = "global"

def outer():
    x = "enclosing"
    def inner():
        x = "local"
        print(x)
    inner()

outer()  # local
```

---

## 🔐 `global` и `nonlocal`

### `global` — изменяет глобальную переменную из функции:

```python
count = 0

def inc():
    global count
    count += 1
```

### `nonlocal` — используется во вложенной функции для изменения переменной из внешней функции:

```python
def outer():
    x = 10
    def inner():
        nonlocal x
        x += 1
    inner()
    print(x)  # 11
```

---

## 📍 Локальные и глобальные переменные

- Все переменные, объявленные внутри функции — **локальные**.
- Чтобы использовать глобальную переменную в функции **на запись**, нужно `global`.
- Но на **чтение** глобальную переменную можно использовать свободно:

```python
x = 5

def show():
    print(x)  # можно

def update():
    x += 1     # ошибка! пытаемся изменить без global
```

---

## 📎 Передача по ссылке

Python **не копирует** объекты при присваивании или передаче в функцию. Он передаёт **ссылку**.

### Для неизменяемых объектов (`int`, `str`, `tuple`) это не вызывает проблем:

```python
def modify(x):
    x += 1
    print(x)

a = 5
modify(a)   # 6
print(a)    # 5 — не изменился
```

### Для изменяемых (`list`, `dict`) — может изменить:

```python
def change(lst):
    lst.append(99)

nums = [1, 2, 3]
change(nums)
print(nums)  # [1, 2, 3, 99]
```

---

## 💥 Удаление переменной — `del`

```python
x = 10
del x
print(x)  # ошибка: x не существует
```

---

## 📚 Резюме

| Особенность           | Описание |
|------------------------|----------|
| Присваивание           | Через `=`, без указания типа |
| Динамическая типизация | Тип переменной можно менять |
| Распаковка             | Позволяет присваивать сразу нескольким переменным |
| Скоупы                 | Local → Enclosing → Global → Built-in |
| `global`, `nonlocal`  | Меняют доступ к переменным из других скоупов |
| Передача по ссылке     | Важно для изменяемых объектов |
| `type()`               | Показывает текущий тип значения |

---


---

# 🧠 Что такое Falsy-значения?

В Python любое значение можно использовать в **логическом контексте** (`if`, `while`, `bool()` и т. д.).  

📌 **Falsy (ложные) значения** — это такие значения, которые считаются `False`, когда используются в условии.  

📌 **Truthy (истинные) значения** — всё остальное, что считается `True`.

---

# 📌 Полный список Falsy-значений в Python

| Тип данных  | Falsy-значение |
|------------|---------------|
| `None`     | `None` |
| `bool`     | `False` |
| `int`, `float` | `0`, `0.0` |
| `str`      | `""` (пустая строка) |
| `list`     | `[]` (пустой список) |
| `tuple`    | `()` (пустой кортеж) |
| `set`      | `set()` (пустое множество) |
| `dict`     | `{}` (пустой словарь) |
| `range`    | `range(0)` (пустой диапазон) |

👉 Всё остальное (ненулевые числа, непустые строки, списки и т. д.) — это **Truthy-значения** (`True`).

---

# ✅ Примеры использования Falsy-значений

## 1️⃣ **Falsy в `if`**
```python
value = ""

if value:
    print("Переменная не пуста!")
else:
    print("Переменная пуста!")  # Выполнится, так как value — пустая строка (Falsy)
```

## 2️⃣ **Falsy в `while`**
```python
x = 3

while x:
    print(x)
    x -= 1  # Когда x станет 0, цикл остановится (0 — Falsy)
```

Вывод:
```
3
2
1
```

## 3️⃣ **Проверка Falsy через `bool()`**
```python
print(bool(0))       # False
print(bool([]))      # False
print(bool("Hello")) # True (не пустая строка)
print(bool(42))      # True (ненулевое число)
```

---

# 🔍 Где это полезно?

✅ **Проверка, пустая ли строка**  
```python
name = input("Введите имя: ")

if not name:
    print("Вы не ввели имя!")  # Проверяем пустую строку
```

✅ **Проверка, пуст ли список**  
```python
items = []

if not items:
    print("Список пуст!")  # Falsy, значит условие сработает
```

✅ **Значение по умолчанию через `or`**  
```python
username = input("Введите имя: ") or "Гость"
print(f"Привет, {username}!")
```
🔹 Если пользователь введёт `""` (пустую строку), то Python возьмёт `"Гость"`.

---

# 🚀 Итог

- **Falsy-значения** считаются `False` в логическом контексте.
- Это: `None`, `False`, `0`, `""`, `[]`, `{}`, `()`, `set()`, `range(0)`.
- **Truthy-значения** — всё, что не попадает под Falsy.
- Часто используются для проверок на пустоту.

---
