
---

# 🧠 Что такое тип данных?

**Тип данных (data type)** — это способ описания **вида значения**, с которым работает переменная: это может быть число, текст, логика, список, и т.д.

➡️ Тип данных определяет:
- какие **операции допустимы** (например, + или *),
- как **значение хранится в памяти**,
- как **интерпретировать и использовать** это значение.

---

# 🔢 Числовые типы данных

## 1. `int` — Целое число

### Что это:
Тип `int` представляет целые числа: положительные, отрицательные или 0.  
Примеры:
```python
x = 5
y = -100
z = 0
```

### Где используется:
- Счётчики в цикле `for i in range(10)`
- Арифметические вычисления
- Индексы списка

### Что можно делать:
```python
a = 10
b = 3

print(a + b)   # 13
print(a - b)   # 7
print(a * b)   # 30
print(a // b)  # 3 — целочисленное деление
print(a % b)   # 1 — остаток от деления
print(a ** b)  # 1000 — степень
```

---

## 2. `float` — Число с плавающей точкой

### Что это:
Тип `float` — это дробные (десятичные) числа.  
Примеры:
```python
pi = 3.14
temp = -0.75
```

### Где используется:
- Измерения: температура, расстояние, цена
- Деление чисел
- Физические/финансовые расчёты

### Особенности:
- Всегда с точкой: `2.0`, не `2`
- Не подходит для точной математики с деньгами — там лучше использовать `decimal.Decimal`

---

## 3. `complex` — Комплексные числа

Редко используется в прикладном коде, но поддерживается Python:

```python
z = 3 + 4j
print(z.real)  # 3.0
print(z.imag)  # 4.0
```

---

# 📃 Текстовый тип данных

## 4. `str` — Строка

### Что это:
`str` — это последовательность символов в кавычках (`''` или `""`).

```python
name = "Alice"
text = 'Привет'
```

### Где используется:
- Ввод и вывод (input/output)
- Сообщения пользователю
- Хранение имен, адресов, описаний и т.д.

### Что можно делать:

```python
s = "Python"

print(s.upper())     # 'PYTHON'
print(s.lower())     # 'python'
print(s[0])          # 'P'
print(s[-1])         # 'n'
print(s[1:4])        # 'yth'
print(len(s))        # 6
```

### Особенности:
- Строки **неизменяемы** (immutable) — нельзя изменить по индексу
- Поддерживают методы `.replace()`, `.find()`, `.split()`, `.strip()`, и др.

---

# ✅ Логический тип данных

## 5. `bool` — Булев тип

### Что это:
Только два значения:
```python
True
False
```

### Где используется:
- В `if`, `while`, фильтрации
- Результат сравнений: `5 > 3 → True`

### Пример:
```python
a = 10
b = 5
print(a > b)     # True
print(a == b)    # False
```

### Особенности:
- `True` ведёт себя как `1`, `False` — как `0`
- Булевы значения автоматически участвуют в арифметике:
```python
print(True + True + False)  # 2
```

---

# 🧪 Как определить тип данных?

## 📌 Функция `type()`

```python
print(type(42))          # <class 'int'>
print(type(3.14))        # <class 'float'>
print(type("hello"))     # <class 'str'>
print(type(True))        # <class 'bool'>
```

Используется для отладки, проверки типов, анализа данных.

---

# 🔄 Преобразование (парсинг) данных

## Зачем нужно?
Часто приходится преобразовывать данные из одного типа в другой:
- `input()` всегда возвращает `str`
- нужно сравнить, посчитать, сохранить правильно

## Примеры:

```python
age = int("25")            # строка → число
price = float("19.99")     # строка → дробное
is_valid = bool("abc")     # → True (не пустая строка)
text = str(123)            # число → строка
```

## Важно:
- `bool("")` → `False`
- `bool("0")` → `True` (строка не пустая!)
- `int("abc")` вызовет `ValueError`

---

# 📦 Коллекции (составные типы данных)

## 6. `list` — Список

```python
lst = [1, 2, 3]
lst.append(4)
print(lst[0])  # 1
```

✅ Изменяемый  
✅ Поддерживает дубликаты  
✅ Упорядоченный

---

## 7. `tuple` — Кортеж

```python
t = (1, 2, 3)
```

❌ Неизменяемый  
✅ Быстрее списка  
✅ Можно использовать как ключ словаря

---

## 8. `set` — Множество

```python
s = {1, 2, 2, 3}
print(s)  # {1, 2, 3}
```

❌ Без индексов  
✅ Уникальные элементы  
✅ Полезен для удаления дубликатов

---

## 9. `dict` — Словарь

```python
person = {
    "name": "Alice",
    "age": 25
}
```

✅ Пары ключ:значение  
✅ Быстрый поиск по ключу  
✅ Очень мощный тип — используется повсюду

---

# 🕳️ 10. `NoneType` — `None`

### Что это:
Значение "ничего" или "пусто".

```python
value = None
```

Используется:
- как заглушка
- как результат функции без `return`
- как признак отсутствия данных

---

# 🔍 Как проверить тип и преобразовать

### Пример валидации:

```python
x = input("Введите число: ")

if x.isdigit():
    num = int(x)
    print("Квадрат:", num * num)
else:
    print("Это не число!")
```

---

# 🧠 Общая таблица

| Тип         | Пример            | Особенности                        |
|--------------|------------------|------------------------------------|
| `int`        | `42`, `-1`       | Целые числа                        |
| `float`      | `3.14`, `-0.1`   | Дробные числа                      |
| `complex`    | `1+2j`           | Комплексные                        |
| `bool`       | `True`, `False`  | Логика                             |
| `str`        | `"hello"`        | Строка (текст)                     |
| `list`       | `[1, 2, 3]`      | Изменяемый список                  |
| `tuple`      | `(1, 2, 3)`      | Неизменяемый список                |
| `set`        | `{1, 2, 3}`      | Множество, уникальные значения     |
| `dict`       | `{"a": 1}`       | Словарь (ключи и значения)         |
| `NoneType`   | `None`           | Пустое значение                    |

---

# 📌 Заключение

- Понимание типов данных — основа Python.
- Каждый тип имеет свои методы, особенности и области применения.
- Умение распознавать и преобразовывать типы помогает писать безопасный и надёжный код.

---
